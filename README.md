# Boucherie Sidi Boucif

Site vitrine premium et e-commerce pour la Boucherie Sidi Boucif à Béziers.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Configure environment variables (see `.env.example`):
   - Copy `.env.example` to `.env.local`
   - Set `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`
3. Run the app:
   `npm run dev`

## Credits

Developed and maintained by **Optimutech**.
For contact and inquiries: [optimutech.fr](https://optimutech.fr)
